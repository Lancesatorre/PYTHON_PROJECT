IMPORTANT: 
	- To view the extracted datas in excel format, go to the directory/ folder where this CRD file is located and then click the generated excel file.
	- Set Headless Mode = True: The browser will run in the background (faster, invisible),
	  Set Headless Mode = False: The browser window will pop up so you can see it working. 